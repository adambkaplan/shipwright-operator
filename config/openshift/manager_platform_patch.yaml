apiVersion: apps/v1
kind: Deployment
metadata:
  name: operator
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        env:
          - name: PLATFORM
            value: "openshift"
          - name: USE_MANAGED_WEBHOOK_CERTS
            value: "false"
